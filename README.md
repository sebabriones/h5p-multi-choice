# H5P Multiple Choice (CFRD) 1.0

Fork CFRD de **H5P.MultiChoice** (upstream 1.16.14, `coreApi` 1.23, Lumi).

- `machineName`: `H5P.MultiChoiceCFRD`
- Constructor JS: `H5P.MultiChoiceCFRD`
- Editor: `semantics.json` + widgets en `editorDependencies` (sin editor dedicado)

## Desarrollo

```powershell
npm run sync:lumi
```

Copia la librería a `nuevas-librerias-h5p/H5P.MultiChoiceCFRD-1.0/` y publica dependencias CFRD (`H5P.Instructions`, `H5P.JoubelUICFRD-1.0`, `H5P.QuestionCFRD-1.0`).

## Licencia

MIT — ver archivo `LICENSE` si existe, o metadatos en `library.json`.
